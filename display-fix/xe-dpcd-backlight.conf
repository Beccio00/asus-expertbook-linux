# ASUS ExpertBook Ultra B9406CAA brightness control.
#
# Linux 7.2's PSR and Panel Replay defaults are intentionally left untouched.
# Force only the VESA AUX/DPCD backlight interface: this panel can otherwise
# update the sysfs brightness value without changing its actual luminance.
options xe enable_dpcd_backlight=2
